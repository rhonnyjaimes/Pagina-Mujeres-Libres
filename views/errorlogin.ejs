<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Error de Login</title>
    <link rel="stylesheet" href="/styles.css">
    <style>
        body {
            background-color: #fff;
            color: #000;
            font-family: 'Arial', sans-serif;
            display: flex;
            justify-content: center;
            align-items: center;
            height: 100vh;
            margin: 0;
        }
        .error-container {
            text-align: center;
            max-width: 600px;
        }
        h1 {
            font-size: 2.5rem;
            color: #e91e63;
        }
        p {
            font-size: 1.2rem;
            color: #555;
        }
        .btn {
            display: inline-block;
            margin-top: 20px;
            padding: 10px 20px;
            font-size: 1rem;
            color: #fff;
            background-color: #e91e63;
            border: none;
            border-radius: 5px;
            text-decoration: none;
            cursor: pointer;
            transition: background-color 0.3s;
        }
        .btn:hover {
            background-color: #d81b60;
        }
    </style>
</head>
<body>
    <div class="error-container">
        <h1>⚠️ Oops, algo salió mal ⚠️</h1>
        <p><%= mensaje || "El usuario o la contraseña son incorrectos. Intenta nuevamente." %></p>
        <a href="/login" class="btn">Volver a Iniciar Sesion</a>
    </div>
</body>
</html>
